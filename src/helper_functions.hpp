/*
 * @return Reaction rates.
 */
double epsilon(double T, double rho);

/*
 * @return tau by reading opacity data file specified as constant inside
 * function.
 */
double tau(double T, double R, double rho);
